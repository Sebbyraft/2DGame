package gui;

import java.awt.Graphics;
import java.awt.MouseInfo;
import java.awt.image.ImageObserver;
import java.util.List;

import display.GameWindow;
import structures.Vec2;


public class GuiManager {
	
	public static void render(List<GuiElement> guiElements, Graphics g, ImageObserver observer) {
		for(GuiElement guiElement:guiElements) {
			guiElement.render(g, observer);
		}
	}
	
	public static boolean update(List<GuiElement> guiElements) {
		boolean isSelected = false;
		for(GuiElement guiElement:guiElements) {
			
			float x = (float) (MouseInfo.getPointerInfo().getLocation().getX() - GameWindow.getFramePosition().getX());
			float y = (float) (MouseInfo.getPointerInfo().getLocation().getY() - GameWindow.getFramePosition().getY());
			
			if(guiElement.isSelected(x, y)) {
				guiElement.setSize(new Vec2(guiElement.getSize().getX()+8, guiElement.getSize().getY()+8));
			}
		}
		return isSelected;
	}

}
